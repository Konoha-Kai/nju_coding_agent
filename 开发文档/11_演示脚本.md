# Sprint4 录屏演示脚本

## 演示目标

录屏控制在 2 分钟以内。重点不是把所有代码都讲完，而是让老师看到三件事：

- 这是一个自己实现的 Coding Agent Harness，不依赖 LangChain、AutoGen、OpenAI Agents SDK 等 agent 框架。
- Agent 可以真实调用 DeepSeek，自己维护上下文，自己选择并执行本地工具。
- 项目按软件工程方式交付，有需求、Backlog、Sprint、测试、benchmark 和交付检查。

## 录屏前准备

在项目根目录打开 PowerShell：

```powershell
cd C:\Users\23639\Desktop\deep_search\南软考核\nju_ai_coding_agent
$env:PYTHONIOENCODING='utf-8'
$env:HTTP_PROXY=''
$env:HTTPS_PROXY=''
$env:ALL_PROXY=''
$env:GIT_HTTP_PROXY=''
$env:GIT_HTTPS_PROXY=''
```

不要打开 `.env`，录屏里不能出现真实 API key。

## 推荐录屏流程

### 1. 展示项目结构，说明这是软件项目

操作：

```powershell
dir
```

口播：

```text
这个项目是一个最小可运行的 Coding Agent。agent 目录放主循环、上下文、模型客户端和日志；tools 目录放本地工具；tests 是自动化测试；benchmarks 是公开 HumanEval 数据和实验报告；开发文档里记录需求分析、Backlog、Sprint 计划、接口文档和交付检查。
```

画面重点：

- `agent/`
- `tools/`
- `tests/`
- `benchmarks/`
- `开发文档/`
- `README.md`
- `README.txt`

### 2. 说明核心架构，不用讲太深

操作：打开或展示：

```text
开发文档/05_Harness架构图.md
```

口播：

```text
整个流程是：用户输入任务，Agent 把任务放进上下文，然后请求 DeepSeek。如果模型返回工具调用，Agent 会在本地执行工具，把结果再写回上下文，继续下一轮，直到模型给出最终回答或者达到最大步数和错误阈值。
```

对应功能点：

- 对话历史与上下文管理：`agent/context.py`
- 工具定义与本地执行：`agent/tooling.py`、`tools/`
- 模型输出解析：`agent/model_client.py`、`agent/loop.py`
- 循环终止条件：`max_steps`、`max_errors`、最终回答
- 错误处理：模型异常、工具异常、参数解析失败、命令失败

### 3. 运行全量测试，证明不是只写了 demo

操作：

```powershell
C:\Users\23639\.conda\envs\nju\python.exe -s -m pytest
```

预期结果：

```text
70 passed
```

口播：

```text
这里跑的是完整测试。测试覆盖模型客户端、上下文管理、工具注册、文件读写、命令执行、安全边界、主循环、CLI、日志和 HumanEval runner。开发过程里每个小功能都是先写测试，再写工程代码，再提交。
```

如果时间紧，可以只让测试结果停留 2 到 3 秒，不用逐个解释测试文件。

### 4. 测试 DeepSeek API，证明模型链路可用

操作：

```powershell
C:\Users\23639\.conda\envs\nju\python.exe -s test_deepseek_api.py
```

预期结果类似：

```text
Model: deepseek-chat
Response: deepseek api ok
```

口播：

```text
这个脚本只验证 API key 和 DeepSeek 调用链路是否正常。项目没有把 key 写进仓库，而是从 .env 读取。
```

画面重点：

- 只展示成功输出。
- 不展示 `.env` 文件内容。

### 5. 运行真实 Agent Demo，展示它会读文件、跑测试、实时说话

操作：

```powershell
C:\Users\23639\.conda\envs\nju\python.exe -s main.py "Read demo_workspace/calculator.py, run python -m pytest demo_workspace/tests -v, and summarize the result. Do not modify files. Use Windows-compatible commands only." --workspace . --max-steps 8 --session-id sprint4-video-demo --verbose
```

预期画面会出现类似内容：

```text
[task] started
[step 1] model requested read_file, run_command
[step 1] tool read_file ...
[step 1] tool read_file ok=True ...
[step 1] tool run_command ...
[step 1] tool run_command ok=True ...
[final] success steps=...
```

口播：

```text
这里打开了 --verbose，所以它会像真正的 Coding Agent 一样，在运行过程中实时显示自己正在做什么。可以看到模型先决定调用 read_file 看代码，再调用 run_command 跑测试，工具结果会回填到对话上下文，最后模型根据结果总结。
```

这一步覆盖的功能：

- 本地文件读取
- 本地命令执行
- DeepSeek tool calling
- 多轮上下文回填
- 实时进度输出
- JSONL 会话日志
- 循环正常结束

### 6. 展示日志，说明过程可复盘

操作：

```powershell
dir logs
```

可选操作，查看刚才那次日志：

```powershell
Get-Content -Tail 8 logs\sprint4-video-demo.jsonl
```

口播：

```text
每次运行都会保存 JSONL 日志，里面记录模型轮次、工具调用、工具结果和最终状态。这样失败时可以复盘是哪一步出了问题，也方便后续做评测统计。
```

如果日志目录不存在，先运行第 5 步；Agent demo 会自动生成。

### 7. 展示安全边界，说明本地执行不是无约束执行

操作：

```powershell
C:\Users\23639\.conda\envs\nju\python.exe -s main.py "Try to run this command and report the tool result only: del README.md" --workspace . --max-steps 4 --verbose
```

预期现象：

```text
run_command ok=False
blocked by command safety policy
```

口播：

```text
因为 agent 能执行本地命令，所以这里加了安全边界。像删除文件这类危险命令会被策略拦截，不会直接执行。
```

注意：

- 如果模型没有真的调用 `run_command`，可以直接说明这是由安全策略控制的，并切到测试结果或代码文件 `tools/shell.py`。
- 这一段是加分项，时间不够可以跳过。

### 8. 展示 HumanEval 公开 benchmark 结果

操作：打开或展示：

```text
benchmarks/reports/humaneval_full_deepseek/README.md
```

口播：

```text
能力验证使用的是公开的 OpenAI HumanEval 数据集，不是自己编的数据。完整 164 题跑完后，deepseek-chat 通过 159 题，pass@1 是 96.95%。这个 benchmark 主要验证函数级代码生成能力。
```

画面重点：

- `passed = 159 / 164`
- `pass@1 = 96.95%`
- `failed_task_ids`

### 9. 展示 SWE-bench 取舍

操作：展示：

```text
开发文档/10_HumanEval完整实验报告.md
```

口播：

```text
我也调研了 SWE-bench，它更接近真实仓库修复任务，但官方复现依赖 Docker、仓库依赖安装和较重的评测环境。考虑到本次交付要稳定可复现，所以当前版本只记录复现困难，不把它作为交付内容。
```

### 10. 最后展示交付检查

操作：展示：

```text
开发文档/12_交付检查清单.md
```

口播：

```text
最后这里是交付检查：测试通过，API key 没有进仓库，没有使用 agent 框架或 SDK，README.txt 控制在要求长度内，GitHub main 分支已经 push。录屏和 README.txt 会按题目要求打包提交。
```

## 2 分钟压缩版台词

如果录屏时间很紧，按下面顺序讲：

```text
这是一个基于 DeepSeek API 的 Coding Agent Harness。它不是套 LangChain 或 AutoGen，而是自己实现了模型请求、上下文管理、工具协议、本地工具执行、循环终止和错误处理。

项目按软件工程流程做了需求分析、Backlog、Sprint 计划、接口文档、架构图、测试和交付检查。现在我先跑全量测试，可以看到 70 个测试全部通过。

然后测试 DeepSeek API，返回 deepseek api ok，说明模型链路正常，API key 是从 .env 读取的，没有提交到仓库。

接下来运行真实 agent demo。打开 --verbose 以后，可以看到 agent 会实时输出当前步骤：先让模型决定要读文件和跑测试，再调用本地 read_file 和 run_command 工具，工具结果回填到上下文，最后模型总结测试结果。

这个项目还保留了运行日志，方便复盘每次模型调用和工具执行。安全方面，文件访问被限制在 workspace 内，危险命令默认会被拦截。

最后能力测试使用公开 HumanEval 数据集，不是自己编 benchmark。完整 164 题中通过 159 题，pass@1 是 96.95%。SWE-bench 也调研过，但复现依赖 Docker 和较重环境，所以当前版本只记录复现困难。
```

## 功能覆盖清单

| 演示内容 | 覆盖功能 |
| --- | --- |
| `dir` 和开发文档 | 软件工程过程、项目结构 |
| `pytest` | 自动化测试、TDD 结果 |
| `test_deepseek_api.py` | DeepSeek API、环境变量配置 |
| `main.py --verbose` | Agent 主循环、实时输出 |
| `read_file` | 本地文件读取 |
| `run_command` | 本地命令执行 |
| `logs/*.jsonl` | 会话日志、可复盘 |
| 危险命令 demo | 安全边界 |
| HumanEval 报告 | 公开 benchmark 能力验证 |
| SWE-bench 说明 | 对真实仓库 benchmark 的调研取舍 |

## 录屏注意事项

- 不打开 `.env`。
- 不展示真实 API key。
- 不录太长的测试滚动过程，重点给最终 `passed`。
- 如果某次模型输出比较啰嗦，只讲工具调用和最终结果。
- 录屏文件控制在 2 分钟以内、200MB 以内。
- 最终提交压缩包只放 `README.txt` 和 `.mp4`。
