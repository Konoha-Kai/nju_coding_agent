# Sprint4 交付检查清单

## 交付材料

| 检查项 | 状态 | 说明 |
| --- | --- | --- |
| README.md | Done | 说明项目功能、运行方式、测试方式和 HumanEval 结果 |
| README.txt | Done | 简短提交说明，控制在 1000 汉字以内 |
| 开发文档 | Done | 包含需求、Backlog、Sprint、接口、架构、测试流程和实验报告 |
| 演示脚本 | Done | `开发文档/11_演示脚本.md` |
| HumanEval 报告 | Done | `benchmarks/reports/humaneval_full_deepseek/` |
| SWE-bench 记录 | Done | 仅记录复现困难，不保留实现和数据 |

## 验证命令

全量测试：

```powershell
C:\Users\23639\.conda\envs\nju\python.exe -s -m pytest
```

敏感信息扫描：

```powershell
rg "sk-[A-Za-z0-9]{10,}|sk_[A-Za-z0-9]{10,}" README.md README.txt benchmarks 开发文档
```

Git 状态：

```powershell
git status --short --branch
```

## 当前验证结果

| 检查项 | 当前结果 |
| --- | --- |
| pytest | 70 passed |
| DeepSeek API 测试 | `deepseek api ok` |
| API key 扫描 | 无命中 |
| Agent 框架/SDK 扫描 | 无实现依赖，仅保留禁止使用约束说明 |
| README.txt 长度 | 994 字符，含 Git 仓库地址 |
| Agent demo | `--verbose` 运行成功，demo tests 2 passed |
| Git 状态 | 提交前检查 |
| GitHub push | 提交后执行 |

## 交付约束

- `.env` 不提交。
- 不在 README、开发文档、benchmark 报告、录屏中展示 API key。
- 不提交缓存、日志、临时 workspace。
- 最终以 GitHub `main` 分支为准。
